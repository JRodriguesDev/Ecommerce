model Product {
    id String @id @default(nanoid())
    thumbnail String
    rating Float
    slug String
    title String
    category String
    description String
    price Int
    stock Int
    images String[]

    @@map("products")
}