services:
  app:
    container_name: next
    build: 
      context: ..
      dockerfile: docker/app.dockerfile
    env_file:
      - ../.env.local
    networks:
      - api
    volumes:
      - prisma-migrate:/app/migrate
    ports:
      - 3000:3000
    depends_on:
      - db
  db:
    container_name: postgres
    image: postgres:16
    restart: always
    env_file:
      - ../.env.local
    networks:
      - api
    volumes:
      - postgres-volume:/var/lib/postgresql/dat
    ports:
      - "5432:5432"
  stripe:
    container_name: stripe_cli
    image: stripe/stripe-cli:latest
    env_file:
      - ../.env.local
    command: listen --forward-to host.docker.internal:3000/api/stripe/webhook
    extra_hosts:
      - "host.docker.internal:host-gateway"
networks:
  api:
    driver: bridge
volumes:
  postgres-volume:
  prisma-migrate:


