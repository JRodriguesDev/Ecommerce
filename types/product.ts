export interface Product {
    id: string
    thumbnail: string
    slug: string
    title: string
    category: string
    description: string
    price: number
    stock: number
    images: string[]
}